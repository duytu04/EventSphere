package com.eventsphere.registrations.dto;

/**
 * Placeholder request body for future extensions.
 * Currently the eventId is provided via path so the body can stay empty.
 */
public record RegistrationCreateRequest(String note) {}
