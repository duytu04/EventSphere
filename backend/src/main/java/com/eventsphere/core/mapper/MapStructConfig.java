package com.eventsphere.core.mapper;

/**
 * Central place-holder for MapStruct configuration. The project does not yet
 * ship with MapStruct on the classpath, but keeping this interface allows
 * mappers to reference it once the dependency is enabled.
 */
public interface MapStructConfig {
}
